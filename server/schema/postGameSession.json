{
    "title": "Propose move",
    "type": "object",
    "properties": {
        "myPlayer": {
            "enum": ["w", "b"]
        },
        "move": {
            "type": "string"
        }
    },
    "required": ["myPlayer", "move"]
}
